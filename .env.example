# Notion Integration
NOTION_API_KEY=
NOTION_BLOG_DATABASE_ID=
NOTION_ARTWORK_DATABASE_ID=

# Rebuild endpoints
REBUILD_SECRET=          # Shared secret for /api/rebuild/* endpoints
GITHUB_PAT=              # Fine-grained PAT with Actions write on this repo
GITHUB_REPO_OWNER=       # e.g. your-github-username
GITHUB_REPO_NAME=        # e.g. rjk-portfolio

# Vercel (used by GitHub Actions workflows as repository secrets, not by Next.js routes)
VERCEL_API_TOKEN=        # Vercel token with deploy access
VERCEL_PROJECT_ID=       # Vercel project ID
VERCEL_ORG_ID=           # Vercel organization ID
